{% extends 'base.html.twig' %}

{% block title %}{{ name }}{% endblock %}

{% block body %}
    <h1>{{ name }}</h1>
    <p>Prix : {{ price }}€</p>
    <a href="{{ path('product_index') }}">Retour à la liste</a>
{% endblock %}
